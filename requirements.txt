# Crawler
crawl4ai>=0.9.2
pyyaml>=6.0
beautifulsoup4>=4.12

# Dashboard. Its interface is a React application in web/, built into dashboard/static/ —
# nothing renders HTML here, so there is no template engine.
fastapi>=0.141
uvicorn>=0.52
